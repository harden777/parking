#数据库连接
spring: 
    servlet:
      multipart:
          max-file-size: 10MB
          max-request-size: 10MB
    thymeleaf:
        cache: false
    datasource:
        url: jdbc:mysql://localhost:3306/parkweb?characterEncoding=utf8&useSSL=true
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver
    #mvc:
       # static-path-pattern: /static/**
#输出sql语句
mybatis:
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

#服务器端口
server:
   port: 8081


